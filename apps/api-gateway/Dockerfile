# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @server-template/api-gateway`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t server-template-api-gateway`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system server-template-api-gateway && \
          adduser --system -G server-template-api-gateway server-template-api-gateway

COPY apps/api-gateway/dist server-template-api-gateway/
COPY apps/api-gateway/package.json server-template-api-gateway/
RUN chown -R server-template-api-gateway:server-template-api-gateway .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix server-template-api-gateway --omit=dev -f install

CMD [ "node", "server-template-api-gateway" ]
